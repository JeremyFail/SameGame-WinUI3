<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="SameGame.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:SameGame.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource LayerFillColorDefaultBrush}"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <MenuBar x:Name="MainMenuBar" Grid.Row="0" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <controls:GameBoardControl x:Name="BoardPanel" Grid.Column="0" />

            <controls:ObjectivesPanel
                x:Name="ObjectivesPanel"
                Grid.Column="1"
                Visibility="Collapsed" />

            <ProgressRing
                x:Name="LoadingRing"
                Width="48"
                Height="48"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                IsActive="False"
                Visibility="Collapsed" />
        </Grid>

        <Border
            Grid.Row="2"
            Padding="8,4"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="28">
                    <TextBlock x:Name="CountLabel" FontFamily="Consolas" FontWeight="Bold" />
                    <TextBlock x:Name="PointsLabel" FontFamily="Consolas" FontWeight="Bold" />
                </StackPanel>
                <TextBlock
                    x:Name="TimerLabel"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    FontFamily="Consolas"
                    FontWeight="Bold" />
                <TextBlock
                    x:Name="ScoreLabel"
                    Grid.Column="2"
                    HorizontalAlignment="Right"
                    FontFamily="Consolas"
                    FontWeight="Bold" />
            </Grid>
        </Border>
    </Grid>
</Page>
